name: pnwhs_rmc
description: PNWHS RMC Resident Mobile App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.0
  google_fonts: ^6.1.0
  intl: ^0.19.0
  qr_flutter: ^4.1.0
  pin_code_fields: ^8.0.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
